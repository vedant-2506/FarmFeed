# # Application
# spring.application.name=FarmFeed

# # Server (Render compatible)
# server.port=${PORT:9090}

# # Datasource: use provided DB_URL/credentials (Render) or fallback to local MySQL
# spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/farmfeed}
# spring.datasource.username=localhost
# spring.datasource.password=Root@123
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # JPA
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


# Application
spring.application.name=FarmFeed

# Server (Render compatible)
server.port=${PORT:9090}

# Datasource: use provided DB_URL/credentials (Render) or fallback to local MySQL
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/farmfeed}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=Root@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect