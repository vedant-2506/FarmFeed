server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://farmfeed-db-swamidivate2506-dd7d.f.aivencloud.com:12970/farmfeed?useSSL=true&requireSSL=true&serverTimezone=UTC}
spring.datasource.username=${DB_USER:avnadmin}
spring.datasource.password=${DB_PASS:AVNS_48rXCIjUcdNx94kE_CN}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
```

---

### Step 3 — Verify the file on GitHub BEFORE pushing

Open this URL and check what it currently shows:
```
https://github.com/vedant-2506/FarmFeed/blob/main/farmFeed/src/main/resources/application.properties
